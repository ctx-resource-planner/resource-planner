# .env 
DATABASE_URL=postgresql://postgres:C00lW0rd$$20@localhost:5432/resource_db
# Replace with your actual PostgreSQL username, password, host (localhost), port (5432 usually), and database name
###DATABASE_URL=postgresql://postgres:W00dward20$$@localhost:5432/resource_db

MAIL_SERVER=smtp.office365.com
MAIL_PORT=587
MAIL_USE_TLS=True
MAIL_USE_SSL=False
MAIL_USERNAME=ravindra.sonakiya@clovertex.com 
MAIL_DEFAULT_SENDER="Resource Planner App <ravindra.sonakiya@clovertex.com>" 
# --- Flask Configuration ---
# Keep your existing SECRET_KEY line here if you have one
# Keep your existing FLASK_ENV line here if you have one

# --- Flask-Mail Configuration (for emailing reports) ---
# Configure these based on your PowerShell script details
# MAIL_SERVER=smtp.office365.com
# MAIL_PORT=587
# MAIL_USE_TLS=True # This should be the only MAIL_USE_TLS line
# MAIL_USE_SSL=False

# Make sure this matches the username used in Credential Manager
MAIL_USERNAME=ravindra.sonakiya@clovertex.com # Corrected syntax

# Use the format required by your SMTP server
# Or just the email address: MAIL_DEFAULT_SENDER=ravindra.sonakiya@clovertex.com
MAIL_DEFAULT_SENDER="Resource Planner App <ravindra.sonakiya@clovertex.com>" # Corrected syntax


# MAIL_PASSWORD is NOT stored here. It will be retrieved from Credential Manager by the script.

# --- Security Reminder ---
# Ensure this .env file is NOT committed to version control (.gitignore)
# In production on non-Windows platforms, set MAIL_ variables as system environment variables.